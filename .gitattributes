# Force LF line endings for files that are executed inside Linux containers
# (HuggingFace Jobs entrypoint, Python training scripts).
*.sh   text eol=lf
*.py   text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf
Dockerfile text eol=lf
